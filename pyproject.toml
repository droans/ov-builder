[project]
name = "ov-builder"
version = "0.1.0"
description = "Build OpenVINO source files in a Docker container"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "ty>=0.0.49",
]

[dependency-groups]
dev = [
    "prek>=0.4.5",
    "ruff>=0.15.17",
]
